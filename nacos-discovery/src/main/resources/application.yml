server:
  port: 8002

spring:
  application:
    name: nacos-discovery
  profiles:
    include: web
    active: local

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    shutdown:
      enabled: true
    health:
      show-details: always
