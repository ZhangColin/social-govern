server:
  port: 8001

spring:
  application:
    name: nacos-config
  profiles:
    include: web
    active: local

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    shutdown:
      enabled: true
    health:
      show-details: always
